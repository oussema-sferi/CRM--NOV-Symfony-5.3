{% extends 'base.html.twig' %}

{% block title %}Statistiques Téléprospection{% endblock %}



{% block body %}

    <div class="header pb-6" style="background-color: #EDA200">
        <div class="container-fluid">
            <div class="header-body">
                <div class="row align-items-center py-4">
                    <div class="col-lg-4 col-7">
                        <h6 class="h2 text-white d-inline-block mb-0">CRM</h6>
                        <nav aria-label="breadcrumb" class="d-none d-md-inline-block ml-md-4">
                            <ol class="breadcrumb breadcrumb-links breadcrumb-dark">
                                <li class="breadcrumb-item"><i class="fas fa-phone-volume text-orange"></i></li>
                                <li class="breadcrumb-item">Service Téléprospection</li>
                                <li class="breadcrumb-item active" aria-current="page">Statistiques</li>
                            </ol>
                        </nav>
                    </div>
                    <div class="col-lg-8 col-5 text-right">
                        {#{{ dump(app.session.get('date_filter_value')) }}#}
                        {#<a href="#" class="btn btn-sm btn-neutral">Période</a>#}

                            <div class="form-group" id="normal_period_block">
                                <label for="period" style="color: #fff; font-weight: bold; font-family: 'Times New Roman'">Période : </label>
                                <select class="form-control-sm" name="normal_period" id="normal_period">
                                    <option value="" {% if app.session.get('date_filter_value') is not defined or app.session.get('date_filter_value') is null %} selected {% endif %}>Choisir...</option>
                                    <option value="0" {% if app.session.get('date_filter_value') is defined and app.session.get('date_filter_value') == "0" %} selected {% endif %}>Aujourd'hui</option>
                                    <option value="1" {% if app.session.get('date_filter_value') is defined and app.session.get('date_filter_value') == "1" %} selected {% endif %}>Hier</option>
                                    <option value="7" {% if app.session.get('date_filter_value') is defined and app.session.get('date_filter_value') == "7" %} selected {% endif %}>7 derniers jours</option>
                                    <option value="14" {% if app.session.get('date_filter_value') is defined and app.session.get('date_filter_value') == "14" %} selected {% endif %}>14 derniers jours</option>
                                    <option value="30" {% if app.session.get('date_filter_value') is defined and app.session.get('date_filter_value') == "30" %} selected {% endif %}>30 derniers jours</option>
                                    <option value="60" {% if app.session.get('date_filter_value') is defined and app.session.get('date_filter_value') == "60" %} selected {% endif %}>60 derniers jours</option>
                                    <option value="90" {% if app.session.get('date_filter_value') is defined and app.session.get('date_filter_value') == "90" %} selected {% endif %}>90 derniers jours</option>
                                    <option value="100" {% if app.session.get('date_filter_value') is defined and app.session.get('date_filter_value') == "100" %} selected {% endif %}>Personalisée</option>
                                </select>
                                <a href="{{ path('teleprospecting_stats_filters_notifications') }}" class="btn btn-sm btn-success">Appliquer les filtres</a>
                                <a href="{{ path('teleprospecting_stats_filters_initialization') }}" class="btn btn-sm btn-primary">Réinitialiser les filtres</a>
                            </div>
                        <form action="{{ path('teleprospecting_stats_filters') }}" method="post">
                            <div class="form-group" id="custom_period_block" style="display: none">
                                <input type="text" name="dateFilterValue" value="100" hidden>
                                <label for="start_date" style="font-weight: bolder; font-family: Cambria; color: #fff">De: </label>
                                <input type="date" class="form-control-sm" name="start_date" id="start_date" required>
                                <label for="start_date" style="font-weight: bolder; font-family: Cambria; color: #fff">Jusqu'à: </label>
                                <input type="date" class="form-control-sm" name="end_date" id="end_date" required>
                                <a id="retour_period" class="btn btn-sm btn-neutral" style="margin-left: 10px">Retour</a>
                                <input class="btn btn-sm btn-success" type="submit" value="Appliquer les filtres">
                                <a href="{{ path('teleprospecting_stats_filters_initialization') }}" class="btn btn-sm btn-primary" id="between-date-initialization" style="display: none">Réinitialiser les filtres</a>
                            </div>
                        </form>
                        {#<a href="#" class="btn btn-sm btn-neutral">Filters</a>#}
                    </div>
                </div>
                <!-- Card stats -->
                <div class="row">
                    <div class="col-xl-4 col-md-6">
                        <div class="card card-stats">
                            <!-- Card body -->
                            {% if app.session.get('date_filter_value') == "100" %}
                                {% set start_date = app.session.get('date_filter_start')|date('Y-m-d') %}
                                {% set end_date = app.session.get('date_filter_end')|date('Y-m-d') %}
                            {% endif%}
                            {#{{ dump(app.session.get('date_filter_start')|date()) }}#}
                            <div class="card-body">
                                <div class="row">
                                    <div class="col">
                                        <h5 class="card-title text-uppercase text-muted mb-0">Total Téléprospecteurs</h5>
                                        <span class="h2 font-weight-bold mb-0">{{ count_total_telepros }}</span>
                                    </div>
                                    <div class="col-auto">
                                        <div class="icon icon-shape bg-gradient-yellow text-white rounded-circle shadow">
                                            <i class="fas fa-user-friends"></i>
                                        </div>
                                    </div>
                                </div>
                                {#<p class="mt-3 mb-0 text-sm">
                                    <span class="text-success mr-2"><i class="fa fa-arrow-up"></i> 3.48%</span>
                                    <span class="text-nowrap">Since last month</span>
                                </p>#}
                            </div>
                        </div>
                    </div>
                    <div class="col-xl-4 col-md-6">
                        <div class="card card-stats">
                            <!-- Card body -->
                            <div class="card-body">
                                <div class="row">
                                    <div class="col">
                                        <h5 class="card-title text-uppercase text-muted mb-0">Total Fiches de contact</h5>
                                        <span class="h2 font-weight-bold mb-0">{{ total_clients }}</span>
                                    </div>
                                    <div class="col-auto">
                                        <div class="icon icon-shape bg-gradient-info text-white rounded-circle shadow">
                                            <i class="fas fa-list-alt"></i>
                                        </div>
                                    </div>
                                </div>
                               {# <p class="mt-3 mb-0 text-sm">
                                    <span class="text-success mr-2"><i class="fa fa-arrow-up"></i> 3.48%</span>
                                    <span class="text-nowrap">Since last month</span>
                                </p>#}
                            </div>
                        </div>
                    </div>
                    <div class="col-xl-4 col-md-6">
                        <div class="card card-stats">
                            <!-- Card body -->
                            {% if app.session.get('date_filter_value') is not null %}
                                {% set filter = app.session.get('date_filter_value') %}
                                {% set selected_date = "now"|date_modify("-" ~ filter ~ " day")| date('Y-m-d') %}
                            {% endif %}
                            <div class="card-body">
                                {#{{ dump(selected_date) }}#}
                                <div class="row">
                                    <div class="col">
                                        <h5 class="card-title text-uppercase text-muted mb-0">Fiches de contact Traités</h5>
                                        <span class="h2 font-weight-bold mb-0">
                                            {#{{ dump(processed_clients) }}#}
                                            {#{{ dump(processed_clients[2].calls) }}#}
                                            {#{{ processed_clients }}#}
                                            {#{{ dump(selected_date) }}#}
                                            {% if app.session.get('date_filter_value') is not null %}
                                                {% set processed_clients_counter = 0 %}
                                                {% for client in processed_clients %}
                                                            {% set update_date = client.updatedAt | date('Y-m-d') %}

                                                            {% if filter == "0" or filter == "1" %}
                                                                {% if update_date == selected_date and client.status != 0 %}
                                                                    {% set processed_clients_counter = processed_clients_counter + 1 %}
                                                                {% endif %}
                                                            {% elseif filter == "100" %}
                                                                {% if update_date >= start_date and call_date <= end_date and client.status != 0 %}
                                                                    {% set processed_clients_counter = processed_clients_counter + 1 %}
                                                                {% endif %}
                                                            {% else %}
                                                                {% if update_date >= selected_date and client.status != 0 %}
                                                                    {% set processed_clients_counter = processed_clients_counter + 1 %}
                                                                {% endif%}
                                                            {% endif %}
                                                {% endfor %}
                                                {{ processed_clients_counter }}
                                            {% else %}
                                                {{ processed_clients_count }}
                                            {% endif %}
                                        </span>
                                    </div>
                                    <div class="col-auto">
                                        <div class="icon icon-shape bg-gradient-green text-white rounded-circle shadow">
                                            <i class="fas fa-list-alt"></i>
                                        </div>
                                    </div>
                                </div>
                               {# <p class="mt-3 mb-0 text-sm">
                                    <span class="text-success mr-2"><i class="fa fa-arrow-up"></i> 3.48%</span>
                                    <span class="text-nowrap">Since last month</span>
                                </p>#}
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-xl-4 col-md-6">
                        <div class="card card-stats">
                            <!-- Card body -->
                            <div class="card-body">
                                <div class="row">
                                    <div class="col">
                                        <h5 class="card-title text-uppercase text-muted mb-0">Fiches de contact Non Traités</h5>
                                        <span class="h2 font-weight-bold mb-0">
                                            {#{{ not_processed_clients }}#}
                                            {% if app.session.get('date_filter_value') is not null %}
                                                {{ total_clients - processed_clients_counter }}
                                            {% else %}
                                                {{ not_processed_clients }}
                                            {% endif %}
                                        </span>
                                    </div>
                                    <div class="col-auto">
                                        <div class="icon icon-shape bg-gradient-red text-white rounded-circle shadow">
                                            <i class="fas fa-list-alt"></i>
                                        </div>
                                    </div>
                                </div>
                                {# <p class="mt-3 mb-0 text-sm">
                                    <span class="text-success mr-2"><i class="fa fa-arrow-up"></i> 3.48%</span>
                                    <span class="text-nowrap">Since last month</span>
                                </p>#}
                            </div>
                        </div>
                    </div>

                    <div class="col-xl-4 col-md-6">
                        <div class="card card-stats">
                            <!-- Card body -->
                            {% if app.session.get('date_filter_value') is not null %}
                                {% set filter = app.session.get('date_filter_value') %}
                                {% set selected_date = "now"|date_modify("-" ~ filter ~ " day")| date('Y-m-d') %}
                            {% endif %}
                            <div class="card-body">
                                {#{{ dump(selected_date) }}#}
                                <div class="row">
                                    <div class="col">
                                        <h5 class="card-title text-uppercase text-muted mb-0">Total Appels Qualifiés</h5>
                                        <span class="h2 font-weight-bold mb-0">
                                            {#{{ dump(processed_clients) }}#}
                                            {#{{ dump(processed_clients[2].calls) }}#}
                                            {#{{ processed_clients }}#}
                                            {% if app.session.get('date_filter_value') is not null %}
                                                {% set qualified_calls_counter = 0 %}
                                                    {% for call in total_calls %}
                                                            {% set call_date = call.createdAt | date('Y-m-d') %}
                                                            {% if filter == "0" or filter == "1" %}
                                                                {% if call_date == selected_date %}
                                                                    {% if call.generalStatus == "2" %}
                                                                        {% set qualified_calls_counter = qualified_calls_counter + 1 %}
                                                                    {% endif %}
                                                                {% endif %}
                                                            {% elseif filter == "100" %}
                                                                {% if call_date >= start_date and call_date <= end_date %}
                                                                    {% if call.generalStatus == "2" %}
                                                                        {% set qualified_calls_counter = qualified_calls_counter + 1 %}
                                                                    {% endif %}
                                                                {% endif %}
                                                            {% else %}
                                                                {% if call_date >= selected_date %}
                                                                    {% if call.generalStatus == "2" %}
                                                                        {% set qualified_calls_counter = qualified_calls_counter + 1 %}
                                                                    {% endif %}
                                                                {% endif%}
                                                            {% endif %}
                                                    {% endfor %}
                                                {{ qualified_calls_counter }}
                                            {% else %}
                                                {{ qualified_calls_count }}
                                            {% endif %}
                                        </span>
                                    </div>
                                    <div class="col-auto">
                                        <div class="icon icon-shape bg-gradient-green text-white rounded-circle shadow">
                                            <i class="fas fa-list-alt"></i>
                                        </div>
                                    </div>
                                </div>
                                {# <p class="mt-3 mb-0 text-sm">
                                    <span class="text-success mr-2"><i class="fa fa-arrow-up"></i> 3.48%</span>
                                    <span class="text-nowrap">Since last month</span>
                                </p>#}
                            </div>
                        </div>
                    </div>

                    <div class="col-xl-4 col-md-6">
                        <div class="card card-stats">
                            <!-- Card body -->
                            {% if app.session.get('date_filter_value') is not null %}
                                {% set filter = app.session.get('date_filter_value') %}
                                {% set selected_date = "now"|date_modify("-" ~ filter ~ " day")| date('Y-m-d') %}
                            {% endif %}
                            <div class="card-body">
                                <div class="row">
                                    <div class="col">
                                        <h5 class="card-title text-uppercase text-muted mb-0">Total Appels Non Qualifiés</h5>
                                        <span class="h2 font-weight-bold mb-0">
                                            {% if app.session.get('date_filter_value') is not null %}
                                                {% set not_qualified_calls_counter = 0 %}
                                                {% for call in total_calls %}
                                                    {% set call_date = call.createdAt | date('Y-m-d') %}
                                                    {% if filter == "0" or filter == "1" %}
                                                        {% if call_date == selected_date %}
                                                            {% if call.generalStatus == "1" %}
                                                                {% set not_qualified_calls_counter = not_qualified_calls_counter + 1 %}
                                                            {% endif %}
                                                        {% endif %}
                                                    {% elseif filter == "100" %}
                                                        {% if call_date >= start_date and call_date <= end_date %}
                                                            {% if call.generalStatus == "1" %}
                                                                {% set not_qualified_calls_counter = not_qualified_calls_counter + 1 %}
                                                            {% endif %}
                                                        {% endif %}
                                                    {% else %}
                                                        {% if call_date >= selected_date %}
                                                            {% if call.generalStatus == "1" %}
                                                                {% set not_qualified_calls_counter = not_qualified_calls_counter + 1 %}
                                                            {% endif %}
                                                        {% endif%}
                                                    {% endif %}
                                                {% endfor %}
                                                {{ not_qualified_calls_counter }}
                                            {% else %}
                                                {{ not_qualified_calls_count }}
                                            {% endif %}
                                        </span>
                                    </div>
                                    <div class="col-auto">
                                        <div class="icon icon-shape bg-gradient-red text-white rounded-circle shadow">
                                            <i class="fas fa-list-alt"></i>
                                        </div>
                                    </div>
                                </div>
                                {# <p class="mt-3 mb-0 text-sm">
                                    <span class="text-success mr-2"><i class="fa fa-arrow-up"></i> 3.48%</span>
                                    <span class="text-nowrap">Since last month</span>
                                </p>#}
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row">



                    <div class="col-xl-4 col-md-6">
                        <div class="card card-stats">
                            <!-- Card body -->
                            <div class="card-body">
                                <div class="row">
                                    <div class="col">
                                        <h5 class="card-title text-uppercase text-muted mb-0">Total Appels Supprimés</h5>
                                        <span class="h2 font-weight-bold mb-0">

                                            {% if app.session.get('date_filter_value') is not null %}
                                                {% set deleted_calls_counter = 0 %}
                                                {% for call in total_calls %}
                                                    {% if call.isDeleted == 1 %}
                                                        {% set call_deletion_date = call.deletionDate | date('Y-m-d') %}
                                                        {% if filter == "0" or filter == "1" %}
                                                            {% if call_deletion_date == selected_date %}
                                                                {% set deleted_calls_counter = deleted_calls_counter + 1 %}
                                                            {% endif %}
                                                        {% elseif filter == "100" %}
                                                            {% if call_deletion_date >= start_date and call_deletion_date <= end_date %}
                                                                {% set deleted_calls_counter = deleted_calls_counter + 1 %}
                                                            {% endif %}
                                                        {% else %}
                                                            {% if call_deletion_date >= selected_date %}
                                                                {% set deleted_calls_counter = deleted_calls_counter + 1 %}
                                                            {% endif%}
                                                        {% endif %}
                                                    {% endif %}
                                                {% endfor %}
                                                {{ deleted_calls_counter }}
                                            {% else %}
                                                {{ deleted_calls_count }}
                                            {% endif %}

                                        </span>
                                    </div>
                                    <div class="col-auto">
                                        <div class="icon icon-shape bg-gradient-orange text-white rounded-circle shadow">
                                            <i class="fas fa-list-alt"></i>
                                        </div>
                                    </div>
                                </div>
                                {# <p class="mt-3 mb-0 text-sm">
                                    <span class="text-success mr-2"><i class="fa fa-arrow-up"></i> 3.48%</span>
                                    <span class="text-nowrap">Since last month</span>
                                </p>#}
                            </div>
                        </div>
                    </div>

                    <div class="col-xl-4 col-md-6">
                        <div class="card card-stats">
                            <!-- Card body -->
                            <div class="card-body">
                                <div class="row">
                                    <div class="col">
                                        <h5 class="card-title text-uppercase text-muted mb-0">Total RDV Pris</h5>
                                        <span class="h2 font-weight-bold mb-0">
                                            {#{{ total_appointments_count }}#}
                                            {#{{ dump(selected_date) }}#}
                                            {% if app.session.get('date_filter_value') is not null %}
                                                {% set fixed_appointments_counter = 0 %}
                                                {% for appointment in total_appointments %}
                                                    {% set appointment_date = appointment.createdAt | date('Y-m-d') %}
                                                    {% if filter == "0" or filter == "1" %}
                                                        {% if appointment_date == selected_date %}
                                                            {% set fixed_appointments_counter = fixed_appointments_counter + 1 %}
                                                        {% endif %}
                                                    {% elseif filter == "100" %}
                                                        {% if appointment_date >= start_date and appointment_date <= end_date %}
                                                            {% set fixed_appointments_counter = fixed_appointments_counter + 1 %}
                                                        {% endif %}
                                                    {% else %}
                                                        {% if appointment_date >= selected_date %}
                                                            {% set fixed_appointments_counter = fixed_appointments_counter + 1 %}
                                                        {% endif%}
                                                    {% endif %}
                                                {% endfor %}
                                                {{ fixed_appointments_counter }}
                                            {% else %}
                                                {{ total_appointments_count }}
                                            {% endif %}
                                        </span>
                                    </div>
                                    <div class="col-auto">
                                        <div class="icon icon-shape bg-gradient-green text-white rounded-circle shadow">
                                            <i class="far fa-calendar-check"></i>
                                        </div>
                                    </div>
                                </div>
                                {# <p class="mt-3 mb-0 text-sm">
                                    <span class="text-success mr-2"><i class="fa fa-arrow-up"></i> 3.48%</span>
                                    <span class="text-nowrap">Since last month</span>
                                </p>#}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Page content -->

    <div class="container-fluid mt--6">
        {#<div class="row">
            <div class="col-xl-8">
                <div class="card bg-default">
                    <div class="card-header bg-transparent">
                        <div class="row align-items-center">
                            <div class="col">
                                <h6 class="text-light text-uppercase ls-1 mb-1">Aperçu</h6>
                                <h5 class="h3 text-white mb-0">Evaluation des projets</h5>
                            </div>
                            <div class="col">
                                <ul class="nav nav-pills justify-content-end">
                                    <li class="nav-item mr-2 mr-md-0" data-toggle="chart" data-target="#chart-sales-dark" data-update='{"data":{"datasets":[{"data":[0, 20, 10, 30, 15, 40, 20, 60, 60]}]}}' data-prefix="$" data-suffix="k">
                                        <a href="#" class="nav-link py-2 px-3 active" data-toggle="tab">
                                            <span class="d-none d-md-block">Mois</span>
                                            <span class="d-md-none">M</span>
                                        </a>
                                    </li>
                                    <li class="nav-item" data-toggle="chart" data-target="#chart-sales-dark" data-update='{"data":{"datasets":[{"data":[0, 20, 5, 25, 10, 30, 15, 40, 40]}]}}' data-prefix="$" data-suffix="k">
                                        <a href="#" class="nav-link py-2 px-3" data-toggle="tab">
                                            <span class="d-none d-md-block">Semaine</span>
                                            <span class="d-md-none">W</span>
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <!-- Chart -->
                        <div class="chart">
                            <!-- Chart wrapper -->
                            <canvas id="chart-sales-dark" class="chart-canvas"></canvas>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-xl-4">
                <div class="card">
                    <div class="card-header bg-transparent">
                        <div class="row align-items-center">
                            <div class="col">
                                <h6 class="text-uppercase text-muted ls-1 mb-1">Performance</h6>
                                <h5 class="h3 mb-0">Total RDVs</h5>
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <!-- Chart -->
                        <div class="chart">
                            <canvas id="chart-bars" class="chart-canvas"></canvas>
                        </div>
                    </div>
                </div>
            </div>
        </div>#}
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-header border-0">
                        <div class="row align-items-center">
                            <div class="col">
                                <h3 class="mb-0">Liste des Utilisateurs</h3>
                            </div>
                            <div class="col text-right">
                                <a href="#!" class="btn btn-sm btn-info">Afficher Tout</a>
                            </div>
                        </div>
                    </div>
                    <div class="table-responsive">
                        <!-- Projects table -->
                        <table class="table align-items-center table-flush">
                            <thead class="thead-light">
                            <tr>
                                <th scope="col">Téléprospecteur</th>
                                <th scope="col">Fiches traités</th>
                                <th scope="col">RDVs pris</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% if app.session.get('date_filter_value') is not null %}
                                {% set filter = app.session.get('date_filter_value') %}
                                {% set selected_date = "now"|date_modify("-" ~ filter ~ " day")| date('Y-m-d') %}
                            {% endif %}

                            {#{% set today = "now"|format_datetime('short', 'none') %}#}
                            {% for telepro in all_telepros %}
                            <tr class="clickable-row" data-href="{{ path('statistics_per_user', {'id': telepro.id}) }}">
                                <th scope="row">
                                    {{ telepro.firstName ~ ' ' ~ telepro.lastName }}
                                </th>
                                <td>
                                {#{{ dump(app.session.get('date_filter_value')) }}#}
                                    {#{% if app.session.get('date_filter_value') is not null %}
                                        {% set processed_counter = 0 %}
                                        {% for call in telepro.calls  %}
                                            {% set call_date = call.createdAt | format_datetime('short', 'none') %}
                                                {% if filter == "0" or filter == "1" %}
                                                    {% if call_date == selected_date %}
                                                        {% set processed_counter = processed_counter + 1 %}
                                                    {% endif %}
                                                {% else %}
                                                    {% if call_date >= selected_date %}
                                                        {% set processed_counter = processed_counter + 1 %}
                                                    {% endif%}
                                                {% endif %}
                                        {% endfor %}
                                        {{ processed_counter }}
                                    {% else %}
                                        {{ telepro.calls | length }}
                                    {% endif %}#}
                                    {% if app.session.get('date_filter_value') is not null %}
                                        {% set processed_counter = 0 %}
                                        {% set calls_clients_ids_array = [] %}
                                            {% for call in telepro.calls  %}
                                            {% set calls_clients_ids_array = calls_clients_ids_array|merge([call.client.id]) %}
                                            {% endfor %}

                                            {% set newArray = [] %}
                                            {% for id in calls_clients_ids_array %}
                                                {% if id not in newArray %}
                                                    {% set newArray = newArray|merge([id]) %}
                                                {% endif %}
                                            {% endfor %}

                                            {% for call in telepro.calls  %}
                                                {% set call_date = call.createdAt | date('Y-m-d') %}
                                                {% if call.client.id in newArray %}
                                                    {% if filter == "0" or filter == "1" %}
                                                        {% if call_date == selected_date %}
                                                            {% set processed_counter = processed_counter + 1 %}
                                                            {% set newArray = remove(newArray, call.client.id) %}
                                                        {% endif %}
                                                    {% elseif filter == "100" %}
                                                        {% if call_date >= start_date and call_date <= end_date %}
                                                            {% set processed_counter = processed_counter + 1 %}
                                                            {% set newArray = remove(newArray, call.client.id) %}
                                                        {% endif %}
                                                    {% else %}
                                                        {% if call_date >= selected_date %}
                                                            {% set processed_counter = processed_counter + 1 %}
                                                            {% set newArray = remove(newArray, call.client.id) %}
                                                        {% endif%}
                                                    {% endif %}

                                                {% endif %}
                                            {% endfor %}
                                        {#{{ dump(newArray) }}#}
                                        {{ processed_counter }}
                                    {% else %}
                                        {{ telepro.calls | length }}
                                    {% endif %}
                                </td>
                                <td>
                                    {% set taken_appointments_counter = 0 %}
                                    {% if app.session.get('date_filter_value') is not null %}
                                        {% for call in telepro.calls  %}
                                                {% set call_date = call.createdAt | date('Y-m-d') %}

                                            {% if filter == "0" or filter == "1" %}
                                                {% if call_date == selected_date %}
                                                    {% if call.statusDetails == 7 %}
                                                        {% set taken_appointments_counter = taken_appointments_counter + 1 %}
                                                    {% endif %}
                                                {% endif %}
                                            {% elseif filter == "100" %}
                                                {% if call_date >= start_date and call_date <= end_date %}
                                                    {% if call.statusDetails == 7 %}
                                                        {% set taken_appointments_counter = taken_appointments_counter + 1 %}
                                                    {% endif %}
                                                {% endif %}
                                            {% else %}
                                                {% if call_date >= selected_date %}
                                                    {% if call.statusDetails == 7 %}
                                                        {% set taken_appointments_counter = taken_appointments_counter + 1 %}
                                                    {% endif %}
                                                {% endif %}
                                            {% endif %}
                                        {% endfor %}

                                    {% else %}
                                        {% for call in telepro.calls  %}
                                                {% if call.statusDetails == 7 %}
                                                    {% set taken_appointments_counter = taken_appointments_counter + 1 %}
                                                {% endif %}

                                        {% endfor %}
                                    {% endif %}
                                    {{ taken_appointments_counter }}
                                </td>
                            </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

    </div>


{% endblock %}

{% block additionalJs %}
    <script>
        window.onload = () => {

            let normalPeriod =  $("#normal_period");
            let normalPeriodBlock =  $("#normal_period_block");
            let customPeriod = $("#custom_period_block");
            normalPeriod.on('change', function() {
                if($(this).val() === "100") {
                    $(this).parent().fadeOut(50)
                    customPeriod.fadeIn(1000)
                }
            })
            $('#retour_period').click(function () {
                $(this).parent().fadeOut(50)
                normalPeriodBlock.fadeIn(1000);
                normalPeriod.val("");
                normalPeriod.val({{ app.session.get('date_filter_value') }});
                if(normalPeriod.val() === "100") {
                    normalPeriod.val("");
                }
            })

            //date

            $('#normal_period').hover(function () {
                $(this).css('cursor','pointer');
            })
            $('#normal_period').change(function () {
                /*console.log(this.value == "14")*/
                if(this.value && this.value != "100") {
                    $.ajax({
                        url:"{{ path('teleprospecting_stats_filters') }}",
                        method: "POST",
                        dataType: 'json',
                        data : { dateFilterValue: this.value },
                        success: (e) => {
                            /*location.reload();*/
                            /*console.log(e);*/
                        },
                        error : (f) => {
                            console.log(f);
                        }
                    })
                }


            })
            if(normalPeriod.val() === "100") {
                normalPeriodBlock.fadeOut(0.05)
                customPeriod.fadeIn(0.1)
                $("#start_date").val("{{ app.session.get('date_filter_start')|date('Y-m-d') }}")
                $("#end_date").val("{{ app.session.get('date_filter_end')|date('Y-m-d') }}")
                $("#between-date-initialization").fadeIn(0.1)
            }
            $(".clickable-row").hover(function () {
                $(this).css('cursor', 'pointer');
            });

            $(".clickable-row").dblclick(function () {
                window.location = $(this).data("href");
            });
        }
    </script>
{% endblock %}
